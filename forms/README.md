# Templates only

The two blank AcroForm templates HQ fills at runtime, plus nothing else.

The build scripts (`build_order_form.py`, `build_invoice.py`) and the worked
example (`fill_example.py`, `SportPharm-Order-Form-EXAMPLE.pdf`) are
deliberately **not** here. This site is public, and the example is filled with
a real customer's name, address and email — Kevin Blaske at Oklahoma State.
Those files live in the working copy at `~/Documents/Claude/sportpharm-hq/forms/`.
